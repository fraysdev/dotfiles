# dotfiles

My not so complete dotfiles configuration used for my Arch Linux and WSL2 (Ubuntu). Can be use elsewhere.

## Self-notes
```sh
add     # add new config
cfg     # configure existing config to match new config
chg     # change existing config
fix     # fix broken config
mod     # modify existing config (combination of add & chg inside one file/config)
```
